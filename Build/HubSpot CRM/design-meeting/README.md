# HubSpot Design meeting

Output van de skill **`hubspot-implementation-design`**: design-meeting markdown, Excel (vijf tabbladen), eventueel CSV-import voor HubSpot-projecttaken, en API-logbestanden.

Transcripten blijven in **`Input/Transcripts/`**; er is geen aparte `source/`-map nodig in `Build/`.

Zie `.cursor/skills/hubspot-implementation-design/SKILL.md` voor de volledige workflow.
